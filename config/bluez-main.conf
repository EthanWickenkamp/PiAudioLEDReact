[General]
# Disable HSP/HFP (headset profile) — avoids ofono dependencies
Disable=Headset

# Enable key features for audio sink and connection handling
Enable=Source,Sink,Media,Socket

# Ensure devices auto-connect and trust on pairing
AutoEnable=true
DiscoverableTimeout = 0
PairableTimeout = 0
FastConnectable = true

[Policy]
AutoEnable=true
AutoTrust=true